#include "main.h"
#include <stdio.h>
/**
 * mul - checks for checks for a digit (0 though 9).
 * @a ; a - variable
 * @b ; b - variable
 * Return: addition of a and b
 */
int mul(int a, int b)
{
	return (a * b);
}

