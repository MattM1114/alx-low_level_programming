#include "main.h"
#include <stdio.h>
/**
 * mul - function that multiplies two integers.
 * @a ; a - variable
 * @b ; b - variable
 * Return: addition of a and b
 */
int mul(int a, int b)
{
	return (a * b);
}
