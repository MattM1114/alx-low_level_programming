#include "main.h"

/**
 * leet - encodes a string 1337.
 * @s: The string to be encode.
 * Return: A pointer to the encoded string.
 */
char *leet(char *s)
{
	int a = 0, b;
	char leet[8] = {'O', 'L', '?', 'E'}